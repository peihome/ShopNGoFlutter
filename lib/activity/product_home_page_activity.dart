import 'package:flutter/material.dart';

class ProductHomePageActivity extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Product Home Page'),
      ),
      body: Center(
        child: Text('Product Home Page'),
      ),
    );
  }
}